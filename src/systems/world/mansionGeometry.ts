/** Shared layout constants for collision, walls, and floor. */

export const ROOM_HALF = 5

/** Door opening half-width (3 units total) */
export const DOOR_HALF = 1.5

/** World clamp margin (matches outer wall inset) */
export const MANSION_WORLD_HALF = 17.5

/** Outer perimeter wall thickness (XZ). */
export const MANSION_OUTER_WALL_THICKNESS = 0.28
